# Multi-Report OMV Fork - Git Ignore File
# Personal configuration files (users should copy from .sample)
multi_report_omv_config.txt

# CSV data files and logs (contain personal system data)
statisticalsmartdata_omv.csv
*.csv

# Log files and test results
DS_Logs/
*.log
*.txt

# Temporary files
*.tmp
*.temp
*~
.DS_Store
Thumbs.db

# IDE and editor files
*.swp
*.swo
*~
.vscode/settings.json
.vscode/launch.json
.vscode/tasks.json
.idea/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Backup files
*.bak
*.backup
*.old

# Personal scripts and test files
test_*
temp_*

# Email configuration (may contain sensitive info)
email_config.txt

# -------------------------------------------------------
# VERSION 2.0 SPECIFIC IGNORES
# -------------------------------------------------------

# Installation environment file (contains user-specific paths)
version2/.env
.env

# Installation log (contains installation history)
version2/installation.log
installation.log

# Configuration files (auto-generated by script)
version2/config/defaults.conf
version2/config/multi-report-omv.conf
config/defaults.conf
config/multi-report-omv.conf

# Configuration directory (entire folder contains user-specific settings)
version2/config/
config/

# Configuration backups (may contain sensitive data)
version2/config/backups/
config/backups/

# Selftest plugin data (contains user drive information and test history)
version2/config/selftest/
config/selftest/

# Log files
version2/logs/*.log
logs/*.log

# Temporary runtime files
version2/tmp/
tmp/

# Data files (contains user-specific drive data, statistics, history, backups)
version2/data/
data/

# Personal workspace settings (can contain local paths)
*.code-workspace